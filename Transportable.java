 package lab1;

public interface Transportable {

	// Sets class variable to true or false. True if the object is being transported, false if not.
	public void setTransported(boolean transport);
	
	// Returns a boolean of whether or not the object is being transported.
	public boolean getTransported();
	
}
